#! /bin/python3

import parser

#
#	trasforms atoms' list to tokens' list
def tokenize(atoms):
	res = []
	atom_pt = 0
	atom_count = len(atoms)
	
	while curr_atom < atoms_count:
		(atom, row, col) = atoms[atom_ptr]
		if atom: pass
	
	
